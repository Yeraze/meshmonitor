{
  "$schema": "https://schemas.tauri.app/config/2/capability",
  "identifier": "default",
  "description": "Default capabilities for MeshMonitor Desktop",
  "windows": ["*"],
  "permissions": [
    "core:default",
    "opener:default",
    "fs:default",
    "shell:allow-open"
  ]
}
