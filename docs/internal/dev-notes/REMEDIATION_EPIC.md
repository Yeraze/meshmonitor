# Remediation Epic — issue #3962 (Phases 0–5)

**Status doc for the epic orchestration run started 2026-07-06.** Canonical plan: [REMEDIATION_PLAN.md](./REMEDIATION_PLAN.md) / issue [#3962](https://github.com/Yeraze/meshmonitor/issues/3962). This file is the durable state for the `/epic` harness — update it at every phase boundary so a restarted session can resume from the first unchecked phase.

## Scope and decisions (user interview, 2026-07-06)

- **Scope of this run:** Phase 0 (all five tasks) + Phase 1 (all six tasks). Phases 2–5 are future epic runs.
- **PR granularity:** each numbered task is its own PR, per the plan's ground rules.
- **Merge policy:** auto-merge each PR once CI is green and orchestrator review passes (via `/merge`); post a summary per phase.
- **Task 0.2 (trust-proxy):** proceed as planned — default `trust proxy` to `false` when `TRUST_PROXY` is unset, keep the startup warning, document prominently in release notes/README.
- **Plan doc:** merged to main first (this branch) so plan + epic state live on main.

## Phase checklist

Each unchecked phase = one worktree → architect spec → implementation → review → PR → CI → merge cycle.

- [x] **P0-docs** — Merge `docs/remediation-plan` (REMEDIATION_PLAN.md + this file) to main.
- [x] **0.1** — Process-level safety net: `unhandledRejection`/`uncaughtException` handlers in `server.ts`, logging with full context and routing through `gracefulShutdown()`. Exit: deliberate `Promise.reject()` in dev logs and shuts down gracefully.
- [x] **0.2** — Trust-proxy default → `false` when `TRUST_PROXY` unset; keep startup warning; release-notes/README documentation for proxied deployments.
- [x] **0.3** — Pin `meshcore.js` fork dependency to a commit SHA; lockfile regenerated; `npm ci` reproducible.
- [x] **0.4** — Strip/gate the hot-path `console.log`s in `src/services/api.ts` (CSRF token status + token prefix on every mutation); kept diagnostics behind a debug flag.
- [x] **0.5** — `@typescript-eslint/no-floating-promises` as `error` for non-test code; violations fixed or explicitly `void`/`.catch()`ed.
- [x] **1.1** — `withSourceScope` fails closed: omitting `sourceId` throws; explicit `ALL_SOURCES` sentinel for the documented global-by-design consumers (channel decryption, estimated positions, automations); full call-site audit.
- [x] **1.2** — Type-check the tests: `tsconfig.tests.json` (full strict — see phase log deviation), wired into CI non-blocking first; flip to blocking once clean.
- [x] **1.3** — Integration-grade route-test harness: in-memory SQLite + real Express app with real `requirePermission`/session/auth wiring; 3–5 representative route test files converted (source-scoped permission coverage); no mass conversion.
- [x] **1.4** — Lint ratchets: `no-explicit-any` → `error` with checked-in baseline; forbid raw `fetch(` in `src/components/**`/`src/pages/**` (baselined); `react-hooks/exhaustive-deps` + `prefer-const` → `error`.
- [x] **1.5** — Response-envelope convention: `ok(res, data)` / `fail(res, status, code, msg)` helper for the `{ success, error, code }` envelope; documented in CLAUDE.md; new/modified handlers must use it.
- [x] **1.6** — Schema-drift tripwire: CI test diffing `createTables()` schema vs full migration replay (001→latest), normalized `sqlite_master`, fail on divergence.

## Phase 2 checklist

Phase 2 started 2026-07-07. Decisions carried forward from the Phase 0+1 interview: behavior-preserving; each numbered sub-task is one PR; full suite green before merge.

- [x] **2.1** — `MeshCoreManager implements ISourceManager`: one unified `sourceManagerRegistry` for all source types; `meshcoreRegistry.ts` reduced to a `@deprecated` shim (delete after one release). WP1 (interface+guards+config move) → WP2+WP3a (lifecycle+loop hardening) → WP3b (read-site migration) → WP4 (shim+tests+docs). See `task21_spec.md` and PR on branch `feature/3962-p2-meshcore-isourcemanager`.
- [x] **2.2a** — Heartbeat/status-probing: extract shared service parameterized by `ISourceManager` (plan §2.2 ¶1).
- [x] **2.2b** — Auto-announce: MeshCore + Meshtastic cycles → one `autoAnnounceService` with per-protocol adapters (plan §2.2 ¶2).
- [x] **2.2c** — Auto-responder: `checkAutoResponder` in both managers → shared service (plan §2.2 ¶3).
- [x] **2.2d** — Distance-delete scheduling: MeshCore's `DistanceDeleteScheduler` construction → unified ownership in `services/` (plan §2.2 ¶4).
- [x] **2.3** — Singleton retirement: enumerate legacy-singleton branches in `meshtasticManager.ts`; create a registry-managed default source from env config; reduce `export default` to a pure alias; delete last special-casing (plan §2.3). PR #4017 merged 2026-07-08 (WP4 importer migration deferred → picked up in Phase 4.2a).

## Ordering notes

- Phase 0 tasks are independent; executed serially in numeric order (one phase in flight at a time).
- 0.5 and 1.4 both touch `eslint.config.mjs` — keep sequential.
- Phase 1 tasks run after Phase 0, in numeric order; 1.6 is the prerequisite for future Phase 3.3.


## Phase 3 checklist (started 2026-07-09)

Execution order: 3.1 → 3.5 → 3.2 → 3.3 → 3.4 (3.5 before 3.3 so new migrations use the helpers; 3.4 last, benefits from 3.1).

- [x] **3.1** — Split `misc.ts` by domain (11 domains incl. distanceDeleteLog the plan missed). 3-PR stack: PR1 (#4023) solarEstimates/upgradeHistory/newsCache/backupHistory; PR2 (#4030) autoTraceroute/timeSync/distanceDeleteLog/mapPreferences/themes; PR3 (TBD) packetLog/keyRepair + misc.ts teardown (delete `.misc`/`.miscRepo`/`MiscRepository`). Census correction: 11 domains not 10 (distanceDeleteLog plan-omitted). keyRepair moved verbatim (SQLite-only, Task 3.2 owns parity). Sync twins in packetLog moved verbatim (Task 3.4 burn-down). Lint baseline updated net-zero (misc.ts 44 violations → packetLog.ts 33 + keyRepair.ts 11).
- [x] **3.5** — Migration ergonomics: shared idempotency helpers (new migrations only) + registry-derived count assertion (kills the count()===N merge magnet). PR #4034.
- [x] **3.2** — Key-repair backend parity: port to Drizzle 3-backend. PR #4036.
- [x] **3.3** — Schema bootstrap single source of truth: fresh installs via migration replay; burn down the 1.6 tripwire's 15-entry allowlist (7 createIndexes-only indexes → migration); delete createTables()/createIndexes(); retire tripwire.
- [x] **3.4** — Sync/async burn-down in database.ts: delete sync twins in batches; move compute-heavy stragglers to repos/services; target < ~4k lines. PR1 #4043 (caller migration + straggler/node-cache extraction) + PR2 (TBD — deletion sweep). Landed at 5,418 lines (from 8,412 at task start); <4k gap analysis in phase log. `getSetting`/`getAllSettings`/`setSetting` sync trio retained by design.

**Phase 3 COMPLETE** (2026-07-10): misc.ts deleted (3.1), key-repair 3-backend parity (3.2), bootstrap via migration replay + tripwire retired (3.3), migration ergonomics (3.5), sync-legacy surface deleted (3.4).

## Phase 4 + Phase 5 run (started 2026-07-21)

**Scope decisions (user interview, 2026-07-21):**
- **Run scope:** full Phase 4 + Phase 5. Completing them closes **#3962** (Phase 6 declared ongoing hygiene, tracked by the lint ratchet + conventions, not by this epic) and **#3502** (which = Task 4.1).
- **Reconnect state machine (4.2 final step):** IN scope, done last after leaf extractions; its PR gets the **`system-test` label** (real-hardware validation) plus a dev-container soak.
- **Deprecated shims:** delete BOTH the `meshcoreRegistry.ts` shim and the `meshtasticManager` live Proxy alias (WP4: migrate ~8 importers to `getPrimaryMeshtasticManager()`, delete `export default`) — early in Phase 4.2, before decomposition. The one-release window passed with v4.13.0 (2026-07-15).
- Carried forward from prior runs: each numbered task = one epic phase (may be a small PR stack, per 3.1/3.4 precedent); behavior-preserving; full suite green + typecheck + lint:ci before every PR; auto-merge on green CI + orchestrator review via /merge.

**State at run start:** `server.ts` 6,275 lines / 81 inline handlers (groups: nodes 19, settings 17, admin 17, config 15, messages 6, stragglers 7 — the issue's Phase-1/2 groups already extracted); `meshtasticManager.ts` 14,977; `meshcoreRoutes.ts` 3,933; `App.tsx` 5,373; `SettingsTab.tsx` 2,507; `ConfigurationTab.tsx` 2,689; `DataContext.tsx` already 123 lines (5.1 largely pre-done — census needed); 64 raw `fetch()` sites in components/pages.

### Phase 4 checklist

- [x] **4.1** — `server.ts` → route modules (closes #3502): move the 81 remaining inline handlers into `routes/` by group (nodes, settings→extend settingsRoutes, admin, config, messages→extend messageRoutes, user→extend userRoutes, + stragglers), moving/exporting the ~24 server.ts-local helpers with them; adopt envelope + `requirePermission` unchanged. Target: `server.ts` < ~800 lines (bootstrap, middleware, mounting, error handler, shutdown).
- **⚠ 4.2 target correction (architect, 2026-07-21):** the plan's `meshtasticManager.ts < ~4k` exit criterion is unreachable — ~5,000+ lines are protobuf dispatch/packet processing, a leaf named in neither 4.2a nor 4.2b. Honest landings: ~12,500 after 4.2a (thin delegates), ~10–11k after 4.2b. **USER DECISION (2026-07-21): target restated to `<8k` after 4.2b — no 4.2c protobuf-dispatch phase.** ("<8k is acceptable, and still an improvement from start.") **SUPERSEDED by 4.2b architect measurement (2026-07-22):** <8k is arithmetically unreachable without extracting protobuf dispatch (reconnect surface ~904 ln; `process*` family 3,725 ln; manager 13,175 after 4.2a). **USER DECISION (2026-07-22): accept ~12.5k and DROP the line-count exit criterion** — Phase 4.2 exit is qualitative: 6 subsystems in injected services + explicit connection state machine; protobuf dispatch stays in the manager per the original plan's own definition. See task42b_spec.md §0.2.
- [x] **4.2a** — Shim deletions + `meshtasticManager.ts` leaf extractions: delete `meshcoreRegistry.ts` shim; WP4 (migrate alias importers → `getPrimaryMeshtasticManager()`, delete Proxy alias `export default`); then leaves in order: MQTT proxy bridging, cron/announce delegate cleanup (incl. the announce-scheduler-in-disconnect gap), favorites, admin-ack correlation → `adminTransactionService`, config/channel/LoRa admin → `deviceAdminService`, NodeDB maintenance.
- [x] **4.2b** — Reconnect state machine: boolean flags/latches → explicit state enum with single transition function; every issue-referenced behavior (#3270/#3276 teardown-before-reconnect, #3122 startup grace, want_config rate-limiting) preserved as named transitions with tests. PR carries the `system-test` label. Target: manager < ~4k lines.
- [x] **4.3** — `meshcoreRoutes.ts` (3,933 lines) split by concern. Mechanical; permission/envelope discipline byte-identical.

**Phase 4 COMPLETE (2026-07-22):** 4.1 (server.ts 6,275→1,151, #3502 closed), 4.2a (5 PRs, 6 services), 4.2b (state machine, hardware-validated), 4.3 (meshcore split) — 12 PRs total.

### Phase 5 checklist

- [x] **5.1+5.2** — Census + finish DataContext→TanStack remnants (fold `useTelemetry`/`useVersionCheck` hand-rolled intervals into `refetchInterval`); memoize all context provider values (`useMemo` + stable setters). **Landed 2026-07-22:** census found `useTelemetry` interval-fold already done (no work); the only 5.1 remnant was the dead `useVersionCheck.ts` (imported nowhere) vs. a duplicate live inline `setInterval` effect in App.tsx — rewrote `useVersionCheck` as a TanStack Query hook (`refetchInterval`, `ApiService`, 404-stops-polling preserved) and repointed App.tsx onto it. **5.2 done in full:** all 10 provider values (DataContext, MessagingContext, UIContext, AutomationContext, MapContext, SettingsContext, SourceContext, SaveBarContext, AuthContext, CsrfContext) wrapped in `useMemo`; SettingsContext's ~46 bare setter closures stabilized with `useCallback` so its memo isn't defeated by fresh closures every render. **Deferred to 5.4** (confirmed by consumer audit, not assumed): deleting `DataContext.nodes`/`channels`/`messages`/`channelMessages`/`connectionStatus` (entangled with App.tsx prop-drilling — that untangling *is* 5.4's job), and migrating the four `nodesWith*` telemetry Sets off DataContext onto `useTelemetryNodes()` (audit showed they're prop-drilled through App's filter/memo chains into NodesTab/MessagesTab, not a small safe consumer set).
- [ ] **5.3** — SettingsTab draft-object rewrite (kills the 81-dep `handleSave`); update CLAUDE.md "Adding New Settings" recipe; ConfigurationTab same pattern section-by-section as follow-up PRs in this phase.
- [ ] **5.4** — Converge on react-router; dissolve `App.tsx` (one tab per PR; `setActiveTab` → `navigate()`; delete duplicated URL parsing with the last tab). Target: `App.tsx` < ~500 lines.
- [ ] **5.5** — One fetch paradigm: migrate the 64 raw `fetch()` sites to `ApiService`/query hooks; flip the lint baseline for the raw-fetch ban to zero.
- [ ] **5.6** — CSS containment: nodes.css reorder/annotate (the twice-bitten #3532 cascade trap) + freeze-globals convention note.
- [ ] **Close-out** — docs sweep, CLAUDE.md updates, close #3962 + #3502 with summary comments.

## Phase log

Record per-phase: PR link, deviations from plan, follow-ups.

- **5.4 PR1** — PR #4285 (merged 2026-07-22) (router scaffold + adapter + hash shim + audit proof leaf; 8-PR stack per task54_spec.md). Premise corrections (architect): App is the per-source Meshtastic view mounted at `source/:sourceId/*` (NOT the app root); tabs were HASH-routed (`#nodes`) → hash→path redirect shim mandatory (kept ≥1 release); plan's "hand-rolled URL parsing" = two duplicated `detectBaseUrl` copies (delete in final PR); 15 live tabs + 1 orphan (`themes` in VALID_TABS with no render/nav — REMOVED with rationale comment; theme mgmt lives in SettingsTab behind the unrelated `themes` permission). Shipped: `activeTab` derived from useLocation + `setActiveTab`→navigate in UIContext (adapter, removed in final PR); VALID_TABS moved to types/ui.ts (single source of truth — kills the CLAUDE.md VALID_TABS gotcha); nested Routes with index→nodes; audit tab = first route; shim + adapter tests. **Stage 5 validation (deployed f431c2fc):** `#messages` bookmark → `/messages` redirect ✓; audit route renders ✓; legacy tabs render at path URLs ✓; real tab click navigates path ✓; console clean. Lesson re-learned: synthetic pushState/clicks gave false negatives (missing tab bar) — real navigation/click told the truth (per the #4253 memory). Gates: suite success=true 10,283/0/0 (orchestrator re-run; agent's rtk-piped run hung and was killed); typecheck 0; typecheck:tests 304 = exact main parity (floor drifted again upstream); lint:ci 0.
- **5.3 PR1** — PR #4284 (merged 2026-07-22; SettingsTab draft rewrite + CLAUDE.md recipe). Premise corrections (architect census): real mirrors 49 not 57; handleSave deps ~95 not 81; FOUR giant dep arrays collapse (re-seed effect, change-detection effect, resetChanges, handleSave); mirrors save via 3 channels (prop callbacks/context setters/server-only POST). **Critical constraint honored:** server.settings-persistence.test.ts regex-parses the component source for the literal `const settings = {` — preserved (12/12 green, values from draft). Shipped: useReducer draft + typed updateField; useState 70→24; handleSave deps 95→9; hasChanges = shallowEqual(draft, baseline); unconditional re-seed pinned (existing clobber semantics); SettingsTab's exhaustive-deps:4 baseline entry DELETED (genuine shrink; raw-fetch 3 left for 5.5); CLAUDE.md recipe updated (dep-array steps removed). ConfigurationTab: architect found NO dep-array problem there (23 handlers, zero dep arrays, 2 baselined) — pilot+defer recommended; pilot deferred to a follow-up alongside 5.4+ (recorded deviation: plan's "same pattern section-by-section in this phase" descoped — no correctness win, live device-config risk). Orchestrator fixes: eslint-disable directive repositioned to actually target its line; committed. **Stage 5 validation (deployed 31a3db8a):** Time Format 12h→24h → SaveBar appears (draft-diff hasChanges) → Save → survives full reload with clean re-seed → restored; console clean. Gates: suite success=true 10,224/0/0; typecheck 0; typecheck:tests 303; lint:ci 0.
- **5.1+5.2** — PR #4280 (merged 2026-07-22; context memoization + version-check TanStack fold). Premise corrections (architect census): DataContext's 123 lines is a pure state container, NOT a finished migration — the dual source of truth is fully live (App.tsx's poll effect copies payloads into context state); its deletion is entangled with App prop-drilling → **explicitly deferred to 5.4**. `useTelemetry` already on refetchInterval; standalone `useVersionCheck.ts` was DEAD CODE (App had an inline duplicate). Shipped: all 10 provider values memoized (useMemo + stable setters; consumers untouched); `useVersionCheck` rewritten as TanStack query hook (4h refetchInterval, ApiService, 404-stops-polling) + App inline effect replaced + test rewritten; telemetry-Set migration (item C) deferred with 5.1 remnants to 5.4. Baseline: 3-line genuine shrink (dead test's entry). Orchestrator fix: implementer committed the spec artifact — untracked. **Stage 5 browser validation (deployed b2b6a20b):** login + per-user pref load ✓; live poll updates across snapshots ✓; unified-messages channel switch ✓; map-feature toggle → POST map-preferences 200 (memoized MapContext round-trip) ✓; console zero React errors/render-loops ✓; per-source compose not exercised (unified view read-only; MessagingContext consumers untouched, unit-covered). Gates: full suite success=true 10,208/0/0; typecheck 0; typecheck:tests 303; lint:ci 0.
- **4.3** — PR #4275 (merged 2026-07-22; meshcoreRoutes split; 3,933 → 39-line barrel + 7 concern files + shared module, all <~1,020 ln; 88 routes). Premise corrections: plan's "channels" group doesn't exist — real big groups are config (19) + automation (15); file mostly NOT on envelope helper (11 ok/fail vs 254 raw) — zero conversions per verbatim rule. New `meshcoreRouteShared.ts` (guard/managerFor/VALIDATION/danger-pattern/CLI-timeout/validators/audit — now exported). Hazards handled: mergeParams everywhere; guard-before-subrouters; within-group order preserved; implementer caught a spec-census miss (`MESHCORE_PACKET_MAX_LIMIT` sat at a group boundary — reattached to packets, verified no other cross-group leakage via boundary script). Both meshcore test files byte-unchanged and green (the regression net). Moved-line verification: ~3,878/4,112 added lines detected moves; residue = export keywords/imports/barrel. Full suite success=true 10,207/0/0; typecheck 0; typecheck:tests 303; lint:ci 0 (3 `any` sites relocated net-zero); build OK.
- **4.2b** — PR #4274 (merged 2026-07-22; connection state machine; one PR, 4 commits C1/C2/C3+fix). **Premise corrections (architect census):** "dozens of flags" = actually 4 state booleans (2 link-state + 2 cache-state after the §0.3 amendment); the manager owns NO reconnect retry loop (backoff/grace live in TcpTransport) — the real machine is connection-lifecycle + config-capture + manual-resync; <8k arithmetically unreachable (protobuf `process*` = 3,725 ln) → USER dropped the line criterion (exit = qualitative). **Mid-phase spec flaw caught by implementer halt:** deriving `configCaptureComplete` from a single enum is unrepresentable — passive no-VN disconnect preserves it true while disconnected (#3122, pinned at passiveMode.test.ts:156); architect amended (Option A: capture flags = auxiliary cache-state written ONLY by 4 named actions incl. `preserveConfigCapture`; atomic single-dispatch fixes the handleConnected entry window). Landed: `src/server/meshtastic/connectionStateMachine.ts` (297-ln pure reducer, 6-state enum, 41 zero-mock table tests) + `#state` single source of truth with derived getters + write-shims (test-seed compatibility); lifecycle methods route context→dispatch→actions executed in place; `configCompleteFallbackTimer` leak FIXED (was never cancelled on disconnect); `assertStateConsistent()` dev guard; manager-level UserDisconnected non-regression guard in handleDisconnected (documented extrapolation); new connectionLifecycleMapping.test.ts (10 real-lifecycle tests incl. Probing path + timer-leak regression). All 7 pin-test files UNCHANGED and green. Manager 13,175→13,490 (+315 — plumbing exceeds extraction; line count no longer a criterion). **Process:** first implementer correctly HALTED on the spec flaw (halt→architect-amend→resume worked); second implementer retired after twice violating commit-before-suite (orchestrator committed verified C1); fresh implementer delivered C2/C3. Orchestrator fix: missing afterEach import (runtime-benign via vitest globals). **Soak (live node, deployed 79814308):** cold connect ✓; user disconnect held 75s zero auto-reconnect ✓; reconnect full handshake ✓; manual resync <20s + cooldown ✓; 10-min signature watch: 0 orphan-flap, 0 reset-loop, 0 status churn. Not remotely drivable: physical network pull, on-device config change (system-test CI leg covers real-device connects). Gates: full suite success=true 10,186/0/0; typecheck 0; typecheck:tests 303; lint:ci 0.
- **4.2a PR5** — PR #4271 (merged 2026-07-21; device/remote admin flows; manager 14,102→**13,175**; **Task 4.2a COMPLETE** — total 14,977→13,175 across 5 PRs + 6 new services). Split taken: `deviceAdminService.ts` (14 local setters + edit-session + `buildDeviceConfigFromActual` — moved after dependency check, its private `calculateLoRaFrequency` wrapper deleted as dead code) and `remoteAdminService.ts` (11 remote-fetch flows; split justified by the split-brain hazard — remote caches written by protobuf dispatch, read/cleared by these methods, same shape as pendingAdminAcks). 8 new narrow accessors (several return live Map refs mirroring the pre-existing `getRemoteNodeConfig` idiom). I6 preserved by non-involvement (none of the 25 moved methods touch channel/DM routing; positionChannel + key-repair families green unchanged). Premise correction: local setters have NO ack-service dependency (spec guessed wrong). Baseline relocation includes +17 signature-level `any` duplication across the delegate boundary (traced to verbatim pre-existing sites). **Orchestrator fixes during review (3rd gate-misreport):** agent claimed typecheck:tests 303 but actual was 321 — 18 TS2556 spread-arg errors in the new test files (vi.fn 0-arg inference again, same class as PR3); fixed with rest-arg signatures, 303 restored, 26 service tests green. Full suite success=true 10,135/0/0; typecheck 0; lint:ci 0; build OK.
- **4.2a PR4** — PR #4270 (merged 2026-07-21; admin-ack correlation → `AdminTransactionService` + favorites → `FavoritesService`; manager 14,467→14,102). Ack service preserves exact I5 semantics (waiter registered BEFORE send so fast ACKs can't be missed; timeout deletes map entry + resolves null; `errorReason` number-or-null); inbound `processRoutingErrorMessage` (protobuf dispatch, unmoved) settles via `hasPending`/`resolveByRequestId` through the injected service. FavoritesService directly injects AdminTransactionService (constructed in dependency order in the manager ctor). Fields moved: pendingAdminAcks, favoritesSupportCache, autoFavoritingNodes, autoFavoriteSweepRunning. New tests: adminTransactionService.test.ts (fake-timer timeout/resolve/double-settle/concurrent-keying) + favoritesService.test.ts (support cache, sweep re-entrancy, delegation). **Orchestrator note:** agent's "suite JSON" was console output (bad redirection) — suite re-run by orchestrator: success=true 10,109/0/0. typecheck 0; typecheck:tests 303 (independently counted); lint:ci 0; build not re-checked (no serving-path changes).
- **4.2a PR3** — PR #4269 (merged 2026-07-21; announce scheduler → `AutoAnnounceService` + §5b disconnect-gap fix; manager 14,632→14,467). Moved: `startAnnounceScheduler`/`setAnnounceInterval`/`restartAnnounceScheduler`/`sendAutoAnnouncement`/`previewAnnouncementMessage` + `announceScheduler` field (thin delegates stay; announceRoutes/server.ts callers untouched). Token engine `replaceAnnouncementTokens` UNMOVED per spec option (b) — only private→public for the cross-class call; ack/welcome/geofence/timer sharers untouched. §5b: `disconnect()` now stops the announce scheduler (additive; `userDisconnect()` simplified to the same call); `handleConnected` re-arm verified live by the new I4 pin test (`meshtasticManager.announceLifecycle.test.ts` — no orphaned timer, re-arm+fire on reconnect, driven through real handleConnected/disconnect like the #3247 connectRace technique). New accessor `isRebootMergeInProgress()`. 25-case service unit test. **Orchestrator fix during review:** agent-reported typecheck:tests parity was wrong — 2 real TS2554 mock-arity errors in autoAnnounceService.test.ts (vi.fn 0-arg inference); fixed, 303 restored. Full suite success=true 10,072/0/0; typecheck 0; lint:ci 0 (no baseline edit needed — new files carry zero violations); build OK.
- **4.2a PR2** — PR #4268 (merged 2026-07-21; MQTT bridge helpers + NodeDB maintenance; manager 14,913→14,632). §4a landed **helpers-only** per the spec's sanctioned fallback: the 6 mqttLink methods touch 6 private state fields + registry listener wiring; moving them required widening them all public — declined. 5 pure helpers + 2 consts → `mqttProxyBridge.ts` (32-case test; the deleted `meshtasticManager.packet-filter.test.ts`'s 17 cases all present verbatim + 15 new). §4f moved 5/7 methods → `NodeDbMaintenanceService` (constructor-injected, `import type`); `isNodeInDeviceDb`/`getDeviceNodeNums` kept (deviceNodeNums Set written by out-of-scope protobuf dispatch; only the one `.delete()` bridged). 4 narrow public accessors added (`isDeviceConnected`/`isTransportReady`/`sendLocalAdminPacket`/`removeDeviceNodeNum` — each byte-matches its pre-extraction guard) + `sendWantConfigId` private→public. BIGINT: `normalizeBigInts()` already applied upstream in NodesRepository — no coercion needed in moved code (documented). Incident: serena symbolic edits wrote into the shared main checkout mid-task; fully reverted (verified clean), edits redone in worktree with plain tools — **lesson: don't use serena editing tools from a worktree session**. Full suite success=true 10,045/0/0; typecheck 0; typecheck:tests 303; lint:ci 0 (net-zero relocation); build OK.
- **4.2a PR1** — PR #4267 (merged 2026-07-21; WP1 shim deletion + WP4 alias retirement). `meshcoreRegistry.ts` + test deleted (zero runtime importers; coverage already in meshcoreConfig.test.ts / sourceManagerRegistry.meshcore.test.ts); CLAUDE.md updated. Proxy alias + `export default` deleted from meshtasticManager.ts; `fallbackManager` named export kept verbatim (I1/#4020). Keystone: `resolveSourceManager` internals → `getPrimaryMeshtasticManager(reg) ?? fallbackManager` (never-undefined contract pinned by new resolveSourceManager.test.ts). **Deviation (approved):** spec's "capture + undefined-guard" for direct importers replaced with uniform `?? fallbackManager` — an undefined-guard would 500 on MeshCore-only/env-IP installs where the alias previously served fallbackManager data (#4020-class). firmwareUpdateService: 8 methods capture the manager once per operation (atomicity pin test: getPrimaryMeshtasticManager called exactly once per completeUpdate even if primary changes mid-op). **Census additions found by typecheck:** server.ts had 4 unlisted alias uses (backupScheduler init, 3 settings-callbacks, gracefulShutdown); `backupSchedulerService` needed a resolver-function (`() => getPrimary ?? fallback`) not a snapshot — it holds the ref for process lifetime and a snapshot would go stale on primary-source edit; 6 extra test files had `default:`-shaped mocks (found via typecheck:tests, invisible to grep). 18 manager unit tests migrated `module.default` → `module.fallbackManager` (same object in empty-registry tests); 2 bootstrapSources tests rewritten from Proxy-semantics pins to new-contract tests. S8 global deleted (single consumer messageRoutes purge fallback migrated). Baseline: genuine shrink (server.ts 1→0, messageRoutes 29→28 no-explicit-any); backupSchedulerService `() => any` = count parity with the old `any` field (type tightening left for Phase 6). Full suite success=true 10,688 tests/0 failed/0 suitesFailed; typecheck 0; typecheck:tests 303 parity; lint:ci 0; build OK.
- **4.1 PR3** — PR #4265 (nodes 20 + `/auto-ping/stop` + composition-root teardown; server.ts 2,714→**1,151**; closes #3502). New: `nodesRoutes.ts` ('/'-mounted, incl. cross-resource gates scan-remote-admin=settings/send-key-warning=messages), `staticServing.ts` (`configureStaticServing(app)`; `invalidateHtmlCache` re-exported from server.ts for the settingsRoutes callback seam), `utils/jsonBigIntReplacer.ts` (module-load side-effect import at top of server.ts), `services/autoResponderTriggerMigration.ts`, `nodesRoutes.test.ts` (harness; per-source grant 403/200 + optionalAuth + requireSourceId). **Middleware seam DECLINED** per spec §8 decision rule — three bindings (`sessionMiddleware` → `initializeWebSocket(server, sessionMiddleware)` in the listen callback ~900 lines later; `csrfProtection` → the `/api` mount; `csrfTokenEndpoint` → the route-mount block) are consumed far outside the block, so `configureMiddleware(app)` would need return-value threading = restructuring, not a verbatim move — sanctioned fallback taken; `appMiddleware.ts` extraction (~-300 ln, would reach ~850) logged as a follow-up candidate, revisit during/after 4.2. Landing 1,151 vs plan's <800: honest floor accepted (3.4 precedent). Verification: full suite success=true 10,024/0/0; typecheck 0; typecheck:tests 303 parity; lint:ci 0; build OK. Task 4.1 COMPLETE: #3502's 81 remaining inline handlers all extracted across PRs #4260/#4263/#4265.
- **4.1 PR2** — PR #4263 (settings 17 + admin 17, paired for physical interleaving; server.ts 4,943→2,714). New `adminRoutes.ts` (all `requireAdmin()`; module-private `bytesToBase64` + `AUTO_FAVORITE_DEFAULTS`; 2 dynamic channelUrlService imports re-pathed); settingsRoutes.ts +17 verbatim. New adminRoutes.test.ts (harness; 401/403/200 gate + body round-trip). Moved-line verification: 2,142/2,246 exact moves. Baseline net-zero. Full suite success=true 10,010 passed/0 failed; typecheck 0; typecheck:tests 303 parity; lint:ci 0. `/auto-ping/stop/:nodeNum` deliberately left inline for PR3 (nodesRoutes).
- **4.1 PR1** — PR #4260 (messages 6 + poll 1 + config 15 + user 2 handlers + utils `transformDbMessage.ts`/`resolveSourceConnectionConfig.ts`; server.ts 6,275→4,943). Architect premise corrections: #3502's ~24-helper list stale — only 3 helpers still needed moving (`transformDbMessageToMeshMessage`, `resolveSourceConnectionConfig`, `bytesToBase64`; rest extracted in Phases 1–3 or gone); nodes group is 20 handlers not 19; `<800` target honestly `<1,000` guaranteed / ~686 only if the PR3 middleware-wiring seam proves safe (3.4-style sanctioned fallback otherwise — spec §8). Verbatim moves verified via `git diff --color-moved` (1,301/1,487 added lines exact moves; residue = boilerplate). New harness tests: pollRoutes (incl. #2773 anonymous-redaction pin), userPreferencesRoutes; configRoutes.test.ts converted from hand-rolled mirror to real router. Baseline: net-zero relocation. Note: `typecheck:tests` floor on main drifted 283→303 (unrelated files); PR adds 0 — treat 303 as the current parity number. Full suite: success=true, 9,998 passed, 0 failed, 0 suitesFailed.
- **3.4** — PR1 #4043 (merged) + PR2 (TBD). PR1 (WP0–WP2): migrated the last ~21 external sync-facade callers to async, extracted the 4 compute stragglers (`calculatePacketRates`→telemetry repo, `updateNodeMobility`→`nodeMobilityService`, `getNodeNeedingTraceroute` selection→`autoTracerouteSelectionService`) and the node cache (→`NodeCacheService` with repo write-hooks). PR2 (WP3+WP4, pure deletion sweep): deleted **110 SQLite-only sync facade methods + 16 dead `*Async` twins** from `database.ts` in 5 domain batches (nodes 28+1, telemetry 17+10, messages 22+2, traceroutes 20+1, misc 20+2), then **61 orphaned repo `*Sync`/`*Sqlite` methods** across 8 repo files (nodes 16, messages 13, telemetry 10, traceroutes 9, channels 5, packetLog 4, autoTraceroute 3, ignoredNodes 1). Net PR2 diff: 23 files, +709/−4,428. The 57 internal call sites (async twins + SQLite-bootstrap infra `warmupCaches`/`ensureBroadcastNode`/`runDataMigrations`) were repointed to repo async methods or had the sync facade's SQLite branch folded inline; bootstrap infra remains sync-on-purpose (it runs only inside the SQLite `initialize()` path) and keeps a handful of repo `*Sqlite`/`*Sync` methods alive (`getNodeSqlite`, `upsertNodeSqlite`, `insertRouteSegmentSync`, `getAllTraceroutesSync`, `getAllNodesTelemetryTypesSync`, record-holder trio, `primeCacheSqlite`, `deleteInactiveNodesForSourceSqlite`). **KEEP surface (by design):** `getSetting`/`getAllSettings`/`setSetting` facade sync trio + repo `getSettingSync`/`getAllSettingsSync`/`setSettingSync`/`setSettingsSync`/`deleteAllSettingsSync` — genuine cache-backed synchronous reads (PG/MySQL: in-memory `settingsCache` write-through; SQLite: better-sqlite3 sync via Drizzle), used in hot paths/middleware (`dynamicCsp.ts`) with ~130 callers; converting to async is high-fan-out for zero benefit. Genuine-infra sync also retained (`waitForReady`, pool getters, `close`, `getDatabaseType`, `isDatabaseReady`, `getDatabaseVersion`, `validateThemeDefinition`). Behavior-preserving fixes surfaced by the suite during deletion: `upsertNodeAsync` re-applies the #2601 per-source ignore flag post-write; `nodes.markAllNodesAsWelcomed` UPDATE now source-scoped when `sourceId` passed; `getNodeNeedingTracerouteAsync` always routes through `selectNodeNeedingTraceroute` (legacy sync fallback dropped). **Line count: 8,412 (task start) → 8,052 (post-PR1) → 5,418 (post-PR2) vs the <4k target.** The architect spec's honest verdict anticipated this: firmly-under-4k additionally required collapsing the remaining async dialect-dispatch shim bodies (~150–400 lines) and further facade thinning; the spec's sanctioned fallback ("land at ~4.5k+, file a follow-up rather than rush") was taken. Remaining fat is thin async delegators with PG/MySQL-cache vs SQLite dialect branches — a Phase 4 follow-up candidate, not sync legacy. Ride-alongs: `autoTracerouteSelectionService` triple `Date.now()` consolidated to the single `now`; `nodeMobilityService` debug-log emoji removed; lint baseline ratcheted down (database.ts `no-explicit-any` 128→59, `no-restricted-syntax` 1→0). Verification: typecheck 0 errors; typecheck:tests 283 (at baseline); lint:ci exit 0; build succeeded; full suite success=true, 0 failed, 0 suitesFailed.

- **3.3-WP-A** — PR #4040. Migration 113 (`add_bootstrap_only_indexes`): normalises all 10 allowlisted index divergences via DROP+CREATE (SQLite, case-insensitive), native IF NOT EXISTS (PG), and `createIndexIfMissingMysql` helper (MySQL). New helper `createIndexIfMissingMysql` added to `helpers.ts` with 3 unit tests. `schemaDrift.allowlist.ts` shrunk 15 → 2 (only 2 column-order `sqlMismatch` entries remain). Drift tripwire passes green against the shrunken allowlist. CLAUDE.md migration recipe updated (Task 3.5 patch applied — no hardcoded count, helpers documented). Ride-along: 3.2 PR #4036 recorded in checklist.

- **3.3-WP-B+C** — PR TBD. The switch + tripwire retirement (shipped together; tripwire cannot outlive `createTables`). WP-B: `initialize()` no longer calls `createTables()`/`migrateSchema()`/`createIndexes()` — fresh SQLite installs now go straight into the migration-registry loop (migration 001 is the baseline, `selfIdempotent`, creates the full schema). `runDataMigrations()` relocated after the loop (traceroutes table exists before it runs; on fresh installs it remains a no-op). Deleted ~970 lines: `createTables()` (~770 lines, 20+ tables), `migrateSchema()` (6-line no-op), `createIndexes()` (23-line block). PG/MySQL (`createPostgresSchema`/`createMySQLSchema`) untouched — already replay-only. WP-C: `schemaDrift.test.ts` and `schemaDrift.allowlist.ts` deleted. The 2 remaining `sqlMismatch` column-order entries dissolve automatically: replay order is now the only order (new installs use ALTER-appended order; existing installs were already in that order). Snapshot-baseline contingency formally closed — replay ≈1s on `:memory:`, acceptable for first-boot on disk. The 7 perf indexes were genuinely missing on PG/MySQL before WP-A (SQLite got them from `createIndexes()`; PG/MySQL never did) — WP-A migration 113 is the fix; confirmed net perf win on those backends.

- **3.2** — PR #4036. Premise correction #5 for the epic: the plan stated "the feature silently doesn't exist on two of three backends" — but the `*Async` facade methods in `database.ts` already fully implemented PostgreSQL and MySQL via raw `postgresPool`/`mysqlPool` queries. Declaring the feature SQLite-only would have been a functional regression, not a neutral architectural choice. The real defect was architectural: raw PG/MySQL SQL lived in the `database.ts` facade (violating the raw-SQL-free rule), with the repository knowing only SQLite. Task 3.2 = consolidation with zero migrations: schema parity existed on all three backends via migrations 001/008/027; `oldKeyFragment`/`newKeyFragment` were in the DB but not in the Drizzle schema — adding them is a pure TypeScript change. Changes: (1) WP-A: added `oldKeyFragment`/`newKeyFragment` to all 3 log-table Drizzle schema definitions in `misc.ts` — no DDL, no migration, count()===112 unchanged. (2) WP-B: rewrote `keyRepair.ts` with 6 async Drizzle methods (dropped `Sqlite` suffix; deleted introspection method; retention trim uses portable two-step select→notInArray with empty-array guard; `getNodesNeedingKeyRepairAsync` pushes exhausted filter into SQL; `.returning()` for SQLite/PG, `insertId` for MySQL on INSERT). (3) WP-C: slimmed `database.ts` 6 `*Async` methods to one-line delegations; deleted 6 sync twins (zero external callers confirmed — grep returned single test-mock reference, updated); deleted all raw `postgresPool.query`/`mysqlPool.query`/`better-sqlite3.prepare` key-repair blocks (~250 lines). (4) WP-D: new `keyRepair.multidb.test.ts` (8 SQLite cases always run; PG+MySQL skipped locally). Sync-twin deletion: `logKeyRepairAttempt` was present in one test mock (`node-identity-guards.test.ts` L40); no expectations asserted against it; mock line removed. Verification: `npm run typecheck` clean; `typecheck:tests` 283 (at baseline, gate ≤ 283); `lint:ci` exit 0 (2 rules improved, no regressions); `npm run build` succeeded; full suite success=true, 8525 passed, 0 failed, 623 skipped.

- **3.1 PR3** — PR TBD (packetLog/keyRepair split + misc.ts teardown). New files: `packetLog.ts` (19 methods + PacketLogFilterOptions), `keyRepair.ts` (7 SQLite-only methods), `packetLog.test.ts` (renamed from misc.packetlog.test.ts). Deleted: `misc.ts`, `misc.packetlog.test.ts`, `misc.mapprefs.test.ts` (duplicate of mapPreferences.test.ts, leftover from PR2). End-state assertion in `misc.test.ts` (MiscRepository not exported, misc.ts not on disk, PacketLogRepository/KeyRepairRepository exported). Deviations: (1) lint baseline updated via `npm run lint:baseline` — 44 any-type violations moved net-zero from misc.ts to new files; "zero new violations" intent preserved (no net regressions). Full suite: success=true, 8472 passed, 0 failed, 0 suitesFailed; typecheck clean; typecheck:tests 283 (at baseline); lint:ci exit 0; build succeeded.

- **P0-docs** — PR #3966 merged (plan + epic doc on main). No deviations.
- **0.1** — PR #3967 merged (processSafetyNet.ts + idempotent gracefulShutdown with exitCode param). Deviation: NodeJS.* type annotations replaced with inference/unknown due to ESLint no-undef.
- **0.2** — PR #3968 merged (trust-proxy default → false + docs/CHANGELOG BREAKING entry). No deviations.
- **0.3** — PR #3969 merged (SHA pin b98fc338, matched existing lockfile resolution). No deviations.
- **0.4** — PR #3970 merged (token-prefix leak removed; diagnostics → logger.warn/debug). No guard test added (no api.test.ts exists; nothing asserted on the removed lines).
- **0.5** — PR #3972 merged (418 violations: 409 `void`, 9 `.catch(logger.error)`; rule scoped to src non-test). Deviation: "npm run lint exits 0" unmet — 367 pre-existing errors in tests/-dir files; CI lint is non-blocking (continue-on-error) — make blocking in 1.4. mqttBridgeManager "deferred parent broker attach" test observed flaky in CI (passes on rerun).
- **1.1** — PR #3976 merged (ALL_SOURCES unique-symbol sentinel; runtime throw + Tier-2 required params; 3 real leaks fixed: server.ts getNodeCount refresh, deleteNode→deleteNeighborInfoInvolvingNode, meshtasticManager pending-DM fetch). Review-loop correction: implementer's repo-body `?? ALL_SOURCES` normalization (silent fail-open) reverted; explicit per-call-site decisions instead. CI: Quick Tests outgrew its 15-min cap (#3385 redux) → raised to 25 min in this PR.
  **Follow-up findings (1.1 architect §10, epic backlog):** hand-rolled fail-open `if (sourceId)` filters bypass the helper — all of `meshcore.ts`, `notifications.ts` subscriptions, parts of `channels.ts`, `nodes.ts:getAllNodesSqlite`; legacy `*Sync`/`*Sqlite` twins partially covered (Phase 3.4 deletes them); facade `getNode(nodeNum)` single-source-assumption chain (revisit with Phase 2).
- **1.2** — PR TBD (tsconfig.tests.json + `typecheck:tests` script + non-blocking CI steps in ci.yml/pr-tests.yml). Deviations: (a) kept FULL strict incl. noImplicitAny — plan's "noImplicitAny off" injects ~50 false-positive errors into prod src via evolving-any inference; (b) top-level `tests/` dir NOT included — its files pull @types/node into the program and poison frontend inference (~60 spurious errors). Baseline: 283 errors, all in src test files, 0 prod. Flip-to-blocking when count reaches 0 (burndown is a follow-up; top ~8 files hold ~150 errors).
- **1.3** — PR #3986 merged (createRouteTestApp() real-middleware harness + 4 conversions). Ride-alongs: real prod bug fixed (v1/messages GET 500'd without sourceId under fail-closed — missing ?? ALL_SOURCES) + harness anonymous-user race fix. 3 CodeQL alerts on the test fixture left open for human triage (recommend dismiss as used-in-tests).
- **1.4** — PR #3989 merged (count-based lint ratchet, eslint-baseline.json 410 files/2,515 violations; no-explicit-any/exhaustive-deps/prefer-const → error; raw-fetch ban in components/pages; CI lint now BLOCKING — closes the 0.5 deviation). Census correction: 727 pre-existing errors (468 config false-positives fixed at config level, not baselined).
- **1.5** — PR #3990 merged (ok()/fail() helpers + 2 exemplar conversions + CLAUDE.md convention; ApiService does-not-unwrap constraint documented). No deviations.
- **1.6** — PR TBD (schemaDrift.test.ts + schemaDrift.allowlist.ts; 15-entry allowlist; census confirmed 15 divergences — 10 onlyInBootstrap, 3 onlyInReplay, 2 sqlMismatch — matching the architect spec exactly). Finding flagged for Phase 3.3: 7 createIndexes()-only indexes (idx_messages_createdAt/fromNodeId/toNodeId, idx_nodes_updatedAt, idx_route_segments_distance/recordholder/timestamp) are silently lost on a replay-only fresh install; Phase 3.3 must add migrations creating each before deleting createIndexes(). No deviations.
- **2.1** — PR #4004 merged (unified registry; ISourceManager on both managers; meshcoreRegistry → deprecated shim; ensureMeshCoreManagerStarted helper). Adversarial review (8 finders/11 verifiers) caught a confirmed by-id crash class (meshcore ids reaching Meshtastic-only routes via resolveSourceManager/security routes/purge-node) — fixed at shared-resolver altitude with regression tests. Backlog noted: disconnect() idempotency guard, notifyNewNodeDiscovered name fetch, start() contract hardening.
- **2.2a** — PR #4005 merged (HeartbeatScheduler leaf; MeshCore adopts; premise correction documented — no cross-protocol heartbeat duplication). Premise correction: no cross-protocol heartbeat duplication exists (Meshtastic = transport-level keepalive push; MQTT = none) — reduced scope to a HeartbeatScheduler leaf utility adopted by MeshCore only; reconnect machinery untouched (Phase 4.2). New files: `src/server/services/heartbeatScheduler.ts` (protocol-agnostic scheduler: interval/start/stop/in-flight-guard/pre+post-await connected-gate) + `heartbeatScheduler.test.ts` (8 fake-timer unit tests covering every gate). MeshCore adoption: `startHeartbeat`/`stopHeartbeat` delegate to per-instance HeartbeatScheduler; `runHeartbeatProbe` deleted; `heartbeatTimer`/`heartbeatProbeInFlight` fields removed; `heartbeatProbe`/`onHeartbeatOk` added as manager callbacks; `recordHeartbeatFailure`/`beginReconnect` chain unchanged. New integration test in meshcoreNativeBackend.test.ts: N consecutive probe failures → heartbeat_failed×N + state='reconnecting'.
- **2.2b** — PR #4009 merged (CronOrIntervalScheduler leaf; both managers delegate arming; premise correction: ~40 LOC shared skeleton, adapter service = negative ROI). Ride-along: hotfix PR #4007 fixed main broken by #4004/#4002 merge race.
- **2.2c** — Premise correction #3 (cross-manager duplication ≈0%; real find = Meshtastic matcher copy-pasted into its own test — now extracted so tests guard production; MeshCore intentionally separate). Extracted the ~130-line placeholder-DSL matcher (`{name}` / `{name:regex}` brace-parse → capture regex → param extraction) from `checkAutoResponder` in `meshtasticManager.ts` into `matchAutoResponderPattern()` + `AutoResponderMatch` interface in `src/utils/autoResponderUtils.ts`. `meshtasticManager.ts` now calls `matchAutoResponderPattern` (3-line loop); removed orphaned `normalizedText`. Retargeted `meshtasticManager.autoresponder-regex.test.ts` (621 lines) to import the production function — deleted local `extractParameters`+`testTriggerMatch` copy (lines 16–155), adapted all ~61 assertions to `{matched,params}`; added 1 explicit `{matched:false,params:{}}` edge-case assertion. MeshCore `checkAutoResponder` byte-for-byte unchanged. Full suite: success=true, 0 failed, 0 suitesFailed; typecheck clean; typecheck:tests 283 errors (at baseline); lint:ci exit 0.
- **2.2d** — Premise correction #4 (plan inverted — shared class `DistanceDeleteScheduler` already existed for 3/4 managers; MeshtasticManager was the outlier with inline copy). WP1: `ISourceManager` widened with required `startDistanceDeleteScheduler(): Promise<void>` + `stopDistanceDeleteScheduler(): void` (all 4 concrete managers already implemented them — this is formalization; 4 test stubs updated). WP2: MeshtasticManager migrates from inline `setInterval`/`distanceDeleteInterval` (field + 3 clear sites) to shared `DistanceDeleteScheduler`; fixes latent uncancellable-initial-run bug (discarded setTimeout handle — shared class's `stop()` cancels it). **Deviation from spec field-layout:** `private readonly distanceDeleteScheduler` changed to `private distanceDeleteScheduler: DistanceDeleteScheduler | null = null` with lazy init in `startDistanceDeleteScheduler()` via dynamic `import()`. Root cause: new static import `meshtasticManager → distanceDeleteScheduler` closed a circular chain (`distanceDeleteScheduler → autoDeleteByDistanceService → resolveSourceManager → meshtasticManager`) that Vitest's CJS-like transform cannot resolve with live bindings — 10 test suite collection failures. Lazy `await import()` breaks the static cycle while delivering identical runtime behavior. WP3: `server.ts` duck-casts removed (`as { startDistanceDeleteScheduler?: ... }`) — now typed `ISourceManager` calls; stale "both registries" comment fixed. Ride-along: `meshtasticManager.autoresponder-regex.test.ts` header comment corrected (`src/utils/autoResponderUtils.ts` → `src/server/utils/autoResponderMatcher.ts`). New test: `meshtasticManager.distanceDelete.test.ts` (5 tests: lazy-null assert, delegation via prototype spies, no-op stop, stop-after-start, regression timer-cancel). Full suite: success=true, 8343 passed, 0 failed, 0 suitesFailed; typecheck clean; typecheck:tests 283 (at baseline); lint:ci exit 0 (3 rules improved by cast removal); build succeeded.
- **2.3** — Singleton retirement — **PR #4017 merged** (Phase 2 COMPLETE 2026-07-08). Special-case census dispositions: S1 (constructor default param) kept harmless; S2 (`configureSource()`) **DELETED** — method removed from MeshtasticManager; S3 (first-TCP reuse) **REPLACED** with `makeMeshtastic(id,cfg)` uniform construction + `registry.setPrimaryMeshtasticSource(id)` designation; S4 (env-IP fallback connect) **KEPT** per Q1 (behavior-preserving for all-MeshCore/all-disabled-tcp/autoConnect:false installs); S5 (`getConfig()` env branch) unchanged — branch (b) still serves S4 fallback; S6 (`setNodeIpOverride`/`clearNodeIpOverride`) DEFERRED (per Q2 — global semantics, silent no-op for configured sources, redesign is a follow-up); S7 (`export default`) live Proxy alias (WP2) gains a `set` trap (WP3 safety net — zero property-write consumers found in grep of all 8 importers); WP2+WP3 review (Task 2.3 review) hardened the Proxy `get` trap to bind methods to the resolved primary (`v.bind(p)`) so `this` inside methods is always the concrete instance — not the Proxy — eliminating EventEmitter/class-field re-entry risk and async straddling; S8 `(global as any).meshtasticManager` = alias (unchanged); S9 `resolveSourceManager(null)` = alias transparent (unchanged); S10 runtime-override clear (unchanged); S11 `getStatus().sourceName = sourceId` cosmetic (unchanged). **WP4 (importer migration + export deletion) explicitly DEFERRED** — the Proxy alias keeps all 8 value-importers + `resolveSourceManager` + `messageRoutes` global + `backupSchedulerService` captured-ref correct without per-file edits; deletion planned ≥1 release out per §2.3 plan. Callers with multi-step operations (e.g. firmwareUpdateService disconnect→…→reconnect) should capture `getPrimaryMeshtasticManager()` once per operation when atomicity matters. **Staleness fix (§2.5) already delivered in WP2** — acknowledged as intentional improvement (alias resolves live primary on every access; editing the primary source in the UI no longer strands legacy consumers on a dead manager). **Primary designation lifecycle hardened** (Task 2.3 review FIX 2): `removeManager(id)` now clears `primaryMeshtasticSourceId` to null when the designated primary is removed, so a subsequent `setPrimaryMeshtasticSource` call (e.g. transport-change re-add) is accepted. The insertion-order fallback in `getPrimaryMeshtasticManager` covers the interim window. **passiveMode boot-behavior alignment** (Task 2.3 review FIX 3): the uniform-construction path at boot now forwards `cfg.passiveMode` and `cfg.passiveResyncStaleMs` from DB config to `makeMeshtastic()`, closing a pre-existing gap where the first-source `configureSource()` path forced them off; pinned by new scenario 3b tests. `MeshtasticSourceConfig` widened with `passiveMode` + `passiveResyncStaleMs` to match `new MeshtasticManager(id,cfg)` constructor and sourceRoutes convention (pre-existing gap in the "else" branch, fixed in unison). **announceScheduler-in-disconnect() gap (deferred to Phase 4.2):** `disconnect()` does not stop the announce scheduler, so a reconnect cycle can fire announces while disconnected. A naive fix risks killing announces post-reconnect; this is tracked as a Phase 4.2 item. Pin-test changes: 12 assertions updated across scenarios 1–4 and 8 (configureSource-based → makeMeshtastic-based; fallbackManager-as-primary → first-factory-stub-as-primary); scenario 5 `configureSource NOT called` → `makeMeshtastic NOT called` (equivalent outcome); scenarios 6–7 and S4 assertions unchanged. New tests added: FIX 1 binding test (Proxy method `this` === concrete primary), FIX 2 lifecycle tests (removeManager clears designation; re-designation after clear; fallback scan covers interim), FIX 3 passiveMode pin (scenario 3b: passiveMode:true + passiveResyncStaleMs forwarded at boot; plain row → falsy). Files changed (10): `src/server/bootstrapSources.ts`, `src/server/bootstrapSources.test.ts`, `src/server/meshtasticManager.ts`, `src/server/meshtasticManager.passiveMode.test.ts`, `src/server/meshtasticManager.reconnectAddress.test.ts`, `src/server/applyManagerSettings.ts`, `src/server/server.ts`, `src/server/sourceManagerRegistry.ts`, `src/server/sourceManagerTypes.ts`, `docs/internal/dev-notes/REMEDIATION_EPIC.md`. Full suite: success=true, 0 failed, 0 suitesFailed; typecheck clean; typecheck:tests ≤283; lint:ci exit 0; build succeeded.
